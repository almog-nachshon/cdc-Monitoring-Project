apiVersion: 1

providers:
  - name: "CDC Dashboards"
    orgId: 1
    folder: "CDC"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
